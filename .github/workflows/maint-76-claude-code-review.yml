name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

env:
  CLAUDE_CODE_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

jobs:
  claude-review:
    if: ${{ env.CLAUDE_CODE_TOKEN != '' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ env.CLAUDE_CODE_TOKEN }}
          plugin_marketplaces: https://github.com/anthropics/claude-code.git
          plugins: code-review@claude-code-plugins
          prompt: /code-review:code-review ${{ github.repository }}/pull/${{ github.event.pull_request.number }}

  claude-review-missing-secret:
    if: ${{ env.CLAUDE_CODE_TOKEN == '' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Skip review (secret missing)
        run: |
          cat <<'EOF' >>"$GITHUB_STEP_SUMMARY"
          ### Claude Code Review skipped
          The `CLAUDE_CODE_OAUTH_TOKEN` secret is not configured. Add it to enable automated reviews.
          EOF

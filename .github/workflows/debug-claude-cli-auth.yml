name: "Debug: Claude Code CLI Auth"

# Throwaway workflow to isolate Claude Code CLI auth from keepalive plumbing.
# Delete after debugging is complete.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cli-auth-isolation-test:
    name: "Claude CLI auth (isolated)"
    runs-on: ubuntu-latest
    environment: agent-standard
    timeout-minutes: 10
    steps:
      # ── 1. Environment baseline ──────────────────────────────────
      - name: "1. Environment baseline"
        run: |
          echo "=== Runtime environment ==="
          echo "HOME=$HOME"
          echo "XDG_CONFIG_HOME=${XDG_CONFIG_HOME:-<not set>}"
          echo "CI=${CI:-<not set>}"
          echo "GITHUB_ACTIONS=${GITHUB_ACTIONS:-<not set>}"
          echo "NODE_VERSION=$(node --version 2>&1 || echo 'none')"
          echo "NPM_VERSION=$(npm --version 2>&1 || echo 'none')"
          echo ""
          echo "=== Relevant env vars ==="
          env | grep -iE 'claude|anthropic' || echo "(none)"

      # ── 2. Install Claude Code CLI ───────────────────────────────
      - name: "2. Install Claude Code CLI"
        run: |
          set -euo pipefail
          npm install -g @anthropic-ai/claude-code 2>&1 | tail -5
          echo ""
          echo "Binary: $(which claude 2>&1 || echo 'NOT FOUND')"
          echo "Version: $(claude --version 2>&1 || echo 'unknown')"

      # ── 3. Discover where CLI expects auth ───────────────────────
      - name: "3. Check CLI help for auth clues"
        run: |
          set +e
          echo "=== claude --help (first 60 lines) ==="
          claude --help 2>&1 | head -60
          echo ""
          echo "=== Checking for auth/login/config subcommands ==="
          for cmd in "auth status" "auth whoami" "config list" "whoami"; do
            echo "--- claude $cmd ---"
            claude $cmd 2>&1 || true
            echo ""
          done

      # ── 4. Write auth.json to the standard location ─────────────
      - name: "4. Write auth.json"
        env:
          AUTH_JSON: ${{ secrets.CLAUDE_AUTH_JSON }}
        run: |
          set -euo pipefail
          if [ -z "${AUTH_JSON:-}" ]; then
            echo "::error::CLAUDE_AUTH_JSON secret is empty/missing"
            exit 1
          fi

          config_dir="$HOME/.config/claude"
          mkdir -p "$config_dir"
          printf '%s' "$AUTH_JSON" > "$config_dir/auth.json"
          chmod 0600 "$config_dir/auth.json"

          echo "Written to: $config_dir/auth.json"
          echo "Size: $(wc -c < "$config_dir/auth.json") bytes"
          echo ""

          # Show structure without secrets
          python3 << 'PYEOF'
          import json, os, pathlib
          p = pathlib.Path.home() / ".config" / "claude" / "auth.json"
          data = json.loads(p.read_text())
          def describe(obj, indent=0):
              pfx = "  " * indent
              if isinstance(obj, dict):
                  for k, v in obj.items():
                      if isinstance(v, dict):
                          print(f"{pfx}{k}: (object, {len(v)} keys)")
                          describe(v, indent + 1)
                      elif isinstance(v, str):
                          safe = v[:10] + "..." if len(v) > 12 else v
                          print(f"{pfx}{k}: str len={len(v)} [{safe}]")
                      elif isinstance(v, (int, float)):
                          print(f"{pfx}{k}: {type(v).__name__}={v}")
                      elif isinstance(v, bool):
                          print(f"{pfx}{k}: bool={v}")
                      elif v is None:
                          print(f"{pfx}{k}: null")
                      else:
                          print(f"{pfx}{k}: {type(v).__name__}")
          describe(data)
          PYEOF

      # ── 5. Also write to alternate locations the CLI might check ─
      - name: "5. Write auth.json to alternate config paths"
        env:
          AUTH_JSON: ${{ secrets.CLAUDE_AUTH_JSON }}
        run: |
          set -euo pipefail
          # Some CLI versions may look in ~/.claude/ instead of ~/.config/claude/
          for dir in "$HOME/.claude" "$HOME/.config/claude-code"; do
            mkdir -p "$dir"
            printf '%s' "$AUTH_JSON" > "$dir/auth.json"
            chmod 0600 "$dir/auth.json"
            echo "Also written to: $dir/auth.json"
          done
          echo ""
          echo "=== All config directories ==="
          ls -la "$HOME/.config/claude/" 2>&1 || true
          ls -la "$HOME/.claude/" 2>&1 || true
          ls -la "$HOME/.config/claude-code/" 2>&1 || true

      # ── 6. Check auth status after writing ───────────────────────
      - name: "6. Check auth status after writing config"
        run: |
          set +e
          echo "=== claude auth status ==="
          claude auth status 2>&1 || true
          echo ""
          echo "=== claude whoami ==="
          claude whoami 2>&1 || true
          echo ""
          echo "=== claude config list ==="
          claude config list 2>&1 || true

      # ── 7. Attempt minimal prompt ────────────────────────────────
      - name: "7. Run claude -p (minimal prompt)"
        run: |
          set +e
          echo "=== Attempt 1: claude -p ==="
          timeout 90 claude -p "Respond with exactly the word: AUTH_OK" 2>&1
          echo "Exit code: $?"
          echo ""

          echo "=== Attempt 2: claude -p --max-turns 1 ==="
          timeout 90 claude -p "Respond with exactly the word: AUTH_OK" --max-turns 1 2>&1
          echo "Exit code: $?"
          echo ""

          echo "=== Attempt 3: piped stdin ==="
          echo "Respond with exactly the word: AUTH_OK" | timeout 90 claude 2>&1
          echo "Exit code: $?"

      # ── 8. Check if auth.json was modified by the CLI ────────────
      - name: "8. Check if CLI modified auth.json (token refresh)"
        run: |
          echo "=== Checking for CLI-side token refresh ==="
          for f in "$HOME/.config/claude/auth.json" "$HOME/.claude/auth.json"; do
            if [ -f "$f" ]; then
              echo "$f:"
              echo "  Size: $(wc -c < "$f") bytes"
              echo "  Modified: $(stat -c %y "$f" 2>/dev/null || stat -f %Sm "$f" 2>/dev/null || echo unknown)"
              python3 -c "
          import json
          with open('$f') as fh:
              data = json.load(fh)
          oauth = data.get('claudeAiOauth', {})
          tok = oauth.get('token', '')
          print(f'  token first 10: {tok[:10]}...')
          print(f'  token length: {len(tok)}')
          exp = oauth.get('expiresAt', 'missing')
          print(f'  expiresAt: {exp}')
          " 2>/dev/null || echo "  (could not parse)"
              echo ""
            fi
          done

      # ── 9. Verbose/debug attempt ─────────────────────────────────
      - name: "9. Verbose run for debug output"
        run: |
          set +e
          echo "=== Running with --verbose flag ==="
          timeout 90 claude -p "Say OK" --verbose 2>&1 | head -80
          echo ""
          echo "Exit code: $?"
          echo ""
          echo "=== Check for debug env vars ==="
          echo "Trying CLAUDE_CODE_DEBUG=1..."
          CLAUDE_CODE_DEBUG=1 timeout 90 claude -p "Say OK" 2>&1 | head -80
          echo ""
          echo "Exit code: $?"
          echo ""
          echo "Trying DEBUG=claude:*..."
          DEBUG="claude:*" timeout 90 claude -p "Say OK" 2>&1 | head -80
          echo ""
          echo "Exit code: $?"

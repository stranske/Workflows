# Allowlisted files where actionlint reports "unexpected key \"secrets\" for \"step\"".
# Keep this list tight; any other occurrence should fail dev_check.
templates/consumer-repo/.github/workflows/*.yml

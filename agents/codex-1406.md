<!-- needs-human: workflow change required in .github/workflows/agents-keepalive-loop.yml to gate the "Post review feedback to PR" step. Use the new .github/scripts/keepalive_review_guard.js to evaluate review_result.json (or missing file). Suggested approach: add a step that runs `node .github/scripts/keepalive_review_guard.js review_result.json` and writes `should_post` + `reason` to $GITHUB_OUTPUT; then set `if: steps.review_guard.outputs.should_post == 'true'` on the comment step. If `should_post` is false, log "empty review result" (reason) so workflow logs show the skip. This satisfies the acceptance criteria for skipping on null review, empty string, or all-empty required fields, and when review_result.json does not exist. -->

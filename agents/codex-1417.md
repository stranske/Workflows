<!-- needs-human: workflow updates required. Add explicit `if:` guards on every step/job that posts a PR comment or PR review in .github/workflows/keepalive.yml and .github/workflows/autofix.yml so they cannot run when suppression is active. Gate on the `should_post_review` output from .github/scripts/should-post-review.js. The helper `scripts/generate_suppression_guard_comment.py --include-label` lists candidate steps. -->

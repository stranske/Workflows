# Extraction workspace (can be ignored after extraction complete)
# .extraction/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.venv
env/
venv/
ENV/

# Logs
*.log

# Temporary files
*.tmp
*.bak
*.orig

# Build artifacts (when workflows create them)
.cache/
tmp/

# Codex working files (preserved via workflow artifacts, not git)
# These files are rebuilt each run and would cause merge conflicts
# if multiple PRs commit them. Historical data is preserved in:
# - PR comments (completion summaries)
# - Workflow artifacts (full outputs)
# - Commit messages (change descriptions)
# Generic names (legacy)
codex-prompt.md
codex-output.md
# PR-specific names (to avoid conflicts with concurrent PRs)
codex-prompt-*.md
codex-output-*.md
verifier-context.md

# OS
.DS_Store
Thumbs.db

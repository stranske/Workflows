# Test Fixtures

This directory contains sample assets used by unit and integration tests.

## verifier-pr-diff.patch

A small, synthetic diff used to exercise verifier context parsing in tests or
local tooling. The canonical runtime output for verifier diffs is still written
as verifier-pr-diff.patch at the repository root, but the tracked sample lives
here to keep the repo root clean.
